interface Props {
    companyName: string,
    ticker: string,
    price: number,
}

const Card: React.FC<Props> = ({}: Props): JSX.Element => {
  return (
    <div className="">
    </div>
  )
}

export default Card